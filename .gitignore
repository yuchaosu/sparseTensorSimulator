# ---- Build artifacts ----
*.o
*.exe
objs/
outputs/

# ---- Generated data / results / plots / logs ----
*.out
*.err
*.mpi
*.pdf
*.png
*.npy
*.swp
*.log
*.csv
*.txt
# ...but these tracked reproducibility artifacts are NOT generated output:
!isca/hamlib_provenance.csv
!isca/energy_per_op_28nm.csv
!isca/energy_per_op_1ghz.csv
!isca/ppa_16x16.csv

# ---- Editor / cache ----
.vscode/
__pycache__/

# ---- Kept local, not synced ----
# RTL working area; third-party trees: Ramulator 2.1 (build per RAMULATOR_SETUP.md)
# and the STONNE / DiaBase / Flexagon (SST-STONNE) baselines (build per README).
verilog/
external/
baselines/
